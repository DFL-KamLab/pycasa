# API: I/O

I/O is the entry point into a pycasa workflow. This section covers the public methods that create and populate `Casa` sessions from either your own video files or the packaged default dataset cache.

The same methods are available both as module-level functions (`pc.io.load_video(...)`) and on a session namespace (`self.io.load_video(...)`). Supported video extensions: `.avi`, `.mp4`, `.mov`, `.mkv`, `.flv`, `.wmv`.

!!! note "This page is generated from the code"
    Signatures, parameters, and descriptions below are rendered directly from the
    `pycasa` source docstrings, so they always match the installed version.

---

::: pycasa.casa.io.io_wrapper._SessionIONamespace.load_video

---

::: pycasa.casa.io.io_wrapper._SessionIONamespace.load_default_data
